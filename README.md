# SSP-streamlit

A data analytics project based on a superstore sales dataset visualised in a Streamlit app.

To run the app, download the repo, along with Streamlit (pip install streamlit), and run 'streamlit run ssp_app.py'.
